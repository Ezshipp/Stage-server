import { CancelOrderComponent } from './cancelOrders.component';
import { RouterModule } from '@angular/router';
var CancelOrderR = [
    { path: '', component: CancelOrderComponent }
];
export var CancelOrderRouting = RouterModule.forChild(CancelOrderR);
